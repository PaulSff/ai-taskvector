# Optional: install with `pip install -r units/semantics/requirements.txt`
# LanguageDetector unit uses lingua (Rust-backed, offline).
lingua-language-detector>=2.0.0
markdown-it-py>=3.0.0
Pygments>=2.17.0
spacy>=3.5,<4.0
