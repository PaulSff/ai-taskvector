{
  "units": [
    { "id": "inject", "type": "Inject", "params": {} },
    { "id": "edit", "type": "todo_list", "params": { "action": "remove_task" } }
  ],
  "connections": [
    { "from": "inject", "to": "edit", "from_port": "0", "to_port": "0" }
  ]
}
