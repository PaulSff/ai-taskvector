{
  "units": [
    {
      "id": "inject_graph",
      "type": "Inject",
      "controllable": false,
      "params": {},
      "input_ports": [],
      "output_ports": [
        {"name": "data", "type": "Any"}
      ]
    },
    {
      "id": "todo_list",
      "type": "todo_list",
      "controllable": false,
      "params": {},
      "input_ports": [
        {"name": "data", "type": "Any"},
        {"name": "graph", "type": "Any"}
      ],
      "output_ports": [
        {"name": "graph", "type": "Any"}
      ]
    }
  ],
  "connections": [
    { "from": "inject_graph", "to": "todo_list", "from_port": "0", "to_port": "1" }
  ],
  "metadata": {
    "description": "Apply todo-list edits (add_todo_list, add_task, remove_task, remove_todo_list, mark_completed) to the graph. Default workflow for assistants.tools.todo_manager (tool.yaml). Used by the edit runner for LLM todo edits and by todo_list_manager for client-side chat task injection. Inject (edits) + graph → todo_list → graph."
  }
}
