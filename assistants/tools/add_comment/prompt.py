"""Graph metadata: leave a note on the process graph (commenter is set by the host from the active assistant role)."""

TOOL_ACTION_PROMPT_LINE = (
    '- add_comment: Leave a useful note on the graph: { "action": "add_comment", "info": "..." }'
)
