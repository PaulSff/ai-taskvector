# Git hooks: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  - repo: local
    hooks:
      - id: check-python-syntax
        name: Python syntax (compile)
        entry: python scripts/check_python_syntax.py
        language: system
        types: [python]
        pass_filenames: true
