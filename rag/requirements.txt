# Optional RAG dependencies. Install with: pip install -r rag/requirements.txt
# Use Python 3.10+; runs on CPU (no GPU required).

# Core RAG (Chroma + sentence-transformers; canonical Embedder + ChromaIndexer units)
chromadb>=0.4.0

# Document parsing (PDF, DOC, XLS)
docling>=2.0.0

# Embeddings (CPU-friendly)
sentence-transformers>=2.2.0

# HTTP for fetching catalogues
requests>=2.28.0
