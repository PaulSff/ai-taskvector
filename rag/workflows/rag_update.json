{
  "units": [
    {
      "id": "rag_update",
      "type": "RagUpdate",
      "controllable": false,
      "params": {
        "rag_index_data_dir": "settings.rag_index_data_dir",
        "units_dir": "units",
        "mydata_dir": "settings.mydata_dir",
        "embedding_model": "settings.rag_embedding_model"
      },
      "input_ports": [],
      "output_ports": [
        {"name": "data", "type": "Any"},
        {"name": "error", "type": "str"}
      ]
    }
  ],
  "connections": [],
  "metadata": {
    "description": "Update RAG index (ingest mydata_dir, units, and repo-wide canonical *.json workflows when rag_index_data_dir is under the repo). Params: settings.rag_index_data_dir, units (relative to repo), settings.mydata_dir, settings.rag_embedding_model; optional repo_root to force repository root for workflow scan."
  }
}
