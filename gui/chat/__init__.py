"""agent chat logic for the Flet GUI.

Layout: ``chat.py`` (panel entry); ``ui/`` (Flet layout/rendering); ``session/`` (state + files);
``handlers/`` (agent workflows); ``context/`` (RAG, language, todos, LLM debug); ``role_turns/``;
``parser_follow_up/``; ``utils/``.
"""
