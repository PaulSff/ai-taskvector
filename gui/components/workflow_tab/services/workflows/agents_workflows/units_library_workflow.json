{
  "units": [
    {
      "id": "inject_graph_summary",
      "type": "Inject",
      "controllable": false,
      "params": {},
      "input_ports": [],
      "output_ports": [{"name": "data", "type": "Any"}]
    },
    {
      "id": "units_library",
      "type": "UnitsLibrary",
      "controllable": false,
      "params": {},
      "input_ports": [
        {"name": "graph_summary", "type": "Any"}
      ],
      "output_ports": [
        {"name": "data", "type": "str"},
        {"name": "source_paths", "type": "Any"}
      ]
    }
  ],
  "connections": [
    {"from": "inject_graph_summary", "to": "units_library", "from_port": "0", "to_port": "0"}
  ],
  "metadata": {
    "description": "Graph summary → UnitsLibrary for Add Node dialog. Registry is filled by run_workflow via ensure_full_unit_registry()."
  }
}
