{
  "units": [
    {
      "id": "inject_graph",
      "type": "Inject",
      "controllable": false,
      "params": {},
      "input_ports": [],
      "output_ports": [{"name": "data", "type": "Any"}]
    },
    {
      "id": "replace_graph",
      "type": "replace_graph",
      "controllable": false,
      "params": {},
      "input_ports": [
        {"name": "data", "type": "Any"},
        {"name": "graph", "type": "Any"}
      ],
      "output_ports": [{"name": "graph", "type": "Any"}]
    }
  ],
  "connections": [
    { "from": "inject_graph", "to": "replace_graph", "from_port": "0", "to_port": "1" }
  ],
  "metadata": {
    "description": "Replace the entire graph with a new one. Used by edit runner when LLM outputs replace_graph action (e.g. import)."
  }
}
