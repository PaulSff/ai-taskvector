"""
Workflow tab services: units-library resolution for dialogs (``units_library_types``).

Single-unit workflow runners (summary, diff, load, validate, …) live in
``gui.components.workflow_tab.workflows.core_workflows`` next to the JSON definitions.
Run-console formatting and UI live under ``workflow_tab.console``.
"""
