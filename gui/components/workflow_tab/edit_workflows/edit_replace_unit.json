{
  "units": [
    {
      "id": "inject_graph",
      "type": "Inject",
      "controllable": false,
      "params": {},
      "input_ports": [],
      "output_ports": [{"name": "data", "type": "Any"}]
    },
    {
      "id": "replace_unit",
      "type": "replace_unit",
      "controllable": false,
      "params": {},
      "input_ports": [
        {"name": "data", "type": "Any"},
        {"name": "graph", "type": "Any"}
      ],
      "output_ports": [{"name": "graph", "type": "Any"}]
    }
  ],
  "connections": [
    { "from": "inject_graph", "to": "replace_unit", "from_port": "0", "to_port": "1" }
  ],
  "metadata": {
    "description": "Replace an existing unit by id (type, params, ports). Used by edit runner when LLM outputs replace_unit action."
  }
}
