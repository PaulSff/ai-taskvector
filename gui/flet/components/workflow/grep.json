{
  "units": [
    {
      "id": "grep",
      "type": "grep",
      "controllable": false,
      "params": {
        "pattern": ".",
        "options": "-n"
      },
      "input_ports": [
        {"name": "in", "type": "Any"}
      ],
      "output_ports": [
        {"name": "out", "type": "Any"},
        {"name": "error", "type": "str"}
      ]
    }
  ],
  "connections": [],
  "metadata": {
    "description": "Grep unit: pattern match on input (e.g. log file). Used by Workflow Designer chat when LLM requests grep action; run with inject for log path, returns output to follow-up context."
  }
}
