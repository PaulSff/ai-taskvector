{
  "units": [
    {
      "id": "rag_update",
      "type": "RagUpdate",
      "controllable": false,
      "params": {
        "rag_index_data_dir": "{settings}",
        "units_dir": "units",
        "mydata_dir": "{settings}",
        "embedding_model": "{settings}"
      },
      "input_ports": [],
      "output_ports": [
        {"name": "data", "type": "Any"},
        {"name": "error", "type": "str"}
      ]
    }
  ],
  "connections": [],
  "metadata": {
    "description": "Update RAG index (ingest mydata_dir, units). Used by RAG indexing and chat settings. Single unit RagUpdate; params: rag_index_data_dir, mydata_dir, embedding_model, units_dir."
  }
}
