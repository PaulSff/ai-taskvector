# Full stack (training + GUI). Use Python 3.10–3.12; PyTorch has no wheels for 3.13.
# ollama: Python client for Ollama server (AI chat, RL Coach / Workflow Designer when using local models). Install Ollama app separately; then pip install ollama; ollama pull <model>
ollama>=0.3.0
rule-engine  # Reward rules evaluation (rewards/rules.py)
asteval>=0.9.31  # Reward formula DSL (rewards/formula.py)
# Server (inference_server, rl/llm_inference_server): ASGI app
fastapi>=0.100.0
uvicorn>=0.22.0
# requests: required for Node-RED / EdgeLinkd external adapters (HTTP step endpoint).
# Use 2.32+ to reduce RequestsDependencyWarning with newer urllib3/charset_normalizer.
requests>=2.32.0
# Optional web unit deps (web_search, beautifulsoup, html_to_text, minify_html): pip install -r units/web/requirements.txt
# websocket-client: optional for Node-RED adapter when transport=websocket
websocket-client>=1.6.0
gymnasium>=0.29.0
stable-baselines3[extra]>=2.0.0
tensorboard>=2.11.0
tqdm>=4.65.0
rich>=13.0.0
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
# Analyst / RL Coach: FormulasCalc unit (xlsx). Prefer formulas 1.2+; 1.3.x uses ExcelModel (ExcelCompiler removed).
formulas>=1.2.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pydantic>=2.0.0
pyyaml>=6.0
# Semantics environment: LanguageDetector (offline). Wheels: check Python version on PyPI.
lingua-language-detector>=2.0.0
markdown-it-py>=3.0.0
Pygments>=2.17.0