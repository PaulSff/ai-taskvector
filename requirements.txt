# Full stack (training + GUI). Use Python 3.10–3.12; PyTorch has no wheels for 3.13.
# ollama: Python client for Ollama server (AI chat, text-to-reward). Install Ollama app separately; then pip install ollama; ollama pull llama3.2
ollama>=0.3.0
rule-engine  # Reward rules evaluation (rewards/rules.py)
asteval>=0.9.31  # Reward formula DSL (rewards/formula.py)
# Server (inference_server, rl/llm_inference_server): ASGI app
fastapi>=0.100.0
uvicorn>=0.22.0
# requests: required for Node-RED / EdgeLinkd external adapters (HTTP step endpoint)
requests>=2.28.0
# duckduckgo-search: optional for web_search unit (DuckDuckGo); unit works without it but returns install hint
# duckduckgo-search>=6.0.0
# html2text: optional for html_to_text unit; unit passes through HTML if not installed
# html2text>=2020.1.0
# beautifulsoup4: optional for beautifulsoup unit (HTML parsing, links/tables/text)
# beautifulsoup4>=4.12.0
# minify-html: optional for minify_html unit (reduce HTML size)
# minify-html>=0.15.0
# websocket-client: optional for Node-RED adapter when transport=websocket
websocket-client>=1.6.0
gymnasium>=0.29.0
stable-baselines3[extra]>=2.0.0
tensorboard>=2.11.0
tqdm>=4.65.0
rich>=13.0.0
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pydantic>=2.0.0
pyyaml>=6.0
streamlit>=1.28.0
streamlit-flow-component>=1.6.0

