# Full stack (training + GUI). Use Python 3.10–3.12; PyTorch has no wheels for 3.13.
# Optional: ollama for text-to-reward and chat_with_local_ai (pip install ollama; ollama pull llama3.2)
# Optional: rule-engine for reward rules evaluation (pip install rule-engine)
# requests: required for Node-RED / EdgeLinkd external adapters (HTTP step endpoint)
requests>=2.28.0
gymnasium>=0.29.0
stable-baselines3>=2.0.0
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
pydantic>=2.0.0
pyyaml>=6.0
streamlit>=1.28.0
streamlit-flow-component>=1.6.0

