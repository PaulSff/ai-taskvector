{
  "units": [
    {
      "id": "inject_graph",
      "type": "Inject",
      "controllable": false,
      "params": {},
      "input_ports": [],
      "output_ports": [{"name": "data", "type": "Any"}]
    },
    {
      "id": "add_unit",
      "type": "add_unit",
      "controllable": false,
      "params": {},
      "input_ports": [
        {"name": "data", "type": "Any"},
        {"name": "graph", "type": "Any"}
      ],
      "output_ports": [{"name": "graph", "type": "Any"}]
    }
  ],
  "connections": [
    { "from": "inject_graph", "to": "add_unit", "from_port": "0", "to_port": "1" }
  ],
  "metadata": {
    "description": "Add a unit to the graph (type, id, params, ports). Used by edit runner when LLM outputs add_unit action."
  }
}
